# Pet Shop - Онлайн Зоомагазин

Полнофункциональный интернет-магазин товаров для животных с персональными рекомендациями на основе профиля питомца.

## 🚀 Особенности

### Frontend
- ⚛️ **Next.js 15** с App Router и TypeScript
- 🎨 **Material-UI v6** (Material Design 3) с оранжевой кастомной темой
- 🛒 **Zustand** для state management (корзина, авторизация)
- 📱 **Адаптивный дизайн** для всех устройств
- 🎭 **Framer Motion** для анимаций

### Backend
- 🟢 **Node.js + Express** с TypeScript
- 🔐 **Мок-авторизация** с JWT симуляцией
- 💳 **Интеграция платежей**: ЯндексСплит, СБП, банковские карты (моки)
- 🏢 **Интеграция с 1С** (мок)
- 🐾 **Рекомендательная система** для подбора товаров по характеристикам питомца

### Функционал
1. **Главная страница** - hero секция, категории, популярные товары
2. **Каталог товаров** - фильтры, сортировка, пагинация, 56+ товаров
3. **Корзина** - управление товарами, расчет суммы
4. **Оформление заказа** - контакты, доставка, оплата
5. **Профили питомцев** - создание и управление профилями
6. **Рекомендации** - персональный подбор товаров на основе:
   - Веса питомца → размер порций, одежды
   - Возраста → корм для щенков/взрослых/пожилых
   - Породы → специализированные товары
   - Аллергий → исключение аллергенов
   - Пола → специфичные товары

## 📦 Технологический стек

| Компонент | Технология |
|-----------|------------|
| Frontend | Next.js 15, TypeScript, Material-UI v6 |
| State Management | Zustand |
| Backend | Node.js, Express, TypeScript |
| Данные | JSON (моковые данные) |
| Стилизация | MUI (Material Design 3) |
| Анимации | Framer Motion |
| Контейнеризация | Docker, Docker Compose |

## 🛠️ Установка и запуск

### Предварительные требования
- Node.js 20+
- npm или yarn
- Docker и Docker Compose (опционально)

### Вариант 1: Запуск с Docker Compose (рекомендуется)

```bash
# Клонировать репозиторий
git clone <repository-url>
cd murmurcodex

# Запустить с помощью Docker Compose
docker-compose up --build

# Приложение будет доступно:
# Frontend: http://localhost:3000
# Backend API: http://localhost:5000
```

### Вариант 2: Локальный запуск

#### Backend

```bash
cd backend

# Установить зависимости
npm install

# Создать .env файл
cp .env.example .env

# Запустить в режиме разработки
npm run dev

# Или собрать и запустить production
npm run build
npm start
```

Backend будет доступен на `http://localhost:5000`

#### Frontend

```bash
cd frontend

# Установить зависимости
npm install

# Создать .env.local файл
cp .env.example .env.local

# Запустить в режиме разработки
npm run dev

# Или собрать и запустить production
npm run build
npm start
```

Frontend будет доступен на `http://localhost:3000`

## 📂 Структура проекта

```
murmurcodex/
├── frontend/              # Next.js приложение
│   ├── app/              # App Router страницы
│   │   ├── page.tsx      # Главная страница
│   │   ├── catalog/      # Каталог товаров
│   │   ├── cart/         # Корзина
│   │   ├── checkout/     # Оформление заказа
│   │   ├── pets/         # Профили питомцев
│   │   └── auth/         # Авторизация
│   ├── components/       # React компоненты
│   │   ├── ProductCard.tsx
│   │   ├── PetProfileCard.tsx
│   │   ├── Navigation.tsx
│   │   └── Footer.tsx
│   ├── lib/              # Утилиты и конфигурация
│   │   ├── api.ts        # API клиент
│   │   └── theme.ts      # MUI тема
│   ├── store/            # Zustand stores
│   │   ├── cart.store.ts
│   │   └── auth.store.ts
│   └── types/            # TypeScript типы
│
├── backend/              # Express API сервер
│   ├── src/
│   │   ├── routes/       # API роуты
│   │   ├── controllers/  # Контроллеры
│   │   ├── services/     # Бизнес-логика
│   │   │   ├── recommendation.service.ts  # Рекомендации
│   │   │   ├── product.service.ts
│   │   │   └── pet.service.ts
│   │   ├── types/        # TypeScript типы
│   │   ├── data/         # Моковые данные
│   │   │   ├── products.json   # 56 товаров
│   │   │   ├── categories.json # 8 категорий
│   │   │   └── breeds.json     # 22 породы
│   │   └── index.ts      # Точка входа
│   └── Dockerfile
│
├── docker-compose.yml    # Docker Compose конфигурация
└── README.md
```

## 🎯 API Endpoints

### Товары
- `GET /api/products` - список товаров с фильтрацией
- `GET /api/products/:id` - товар по ID
- `GET /api/categories` - список категорий

### Питомцы
- `GET /api/pets` - список питомцев пользователя
- `POST /api/pets` - создать питомца
- `PUT /api/pets/:id` - обновить питомца
- `DELETE /api/pets/:id` - удалить питомца
- `GET /api/pets/breeds` - список пород

### Рекомендации
- `GET /api/recommendations/:petId` - рекомендации для питомца

### Заказы
- `POST /api/orders` - создать заказ
- `GET /api/orders/:id` - информация о заказе
- `GET /api/orders` - заказы пользователя

### Оплата (моки)
- `POST /api/payment/card` - оплата картой
- `POST /api/payment/yandex-split` - ЯндексСплит
- `POST /api/payment/sbp` - СБП

### 1С (мок)
- `POST /api/1c/sync` - синхронизация с 1С
- `GET /api/1c/status/:orderId` - статус синхронизации

## 🎨 Дизайн и UI/UX

- **Material Design 3** - современный и интуитивный интерфейс
- **Оранжевая акцентная тема** - соответствует брендингу зоомагазина
- **Адаптивный дизайн** - корректное отображение на всех устройствах
- **Skeleton loaders** - улучшенный UX при загрузке
- **Snackbar уведомления** - обратная связь для действий пользователя

## 🐾 Рекомендательная система

Алгоритм подбора товаров учитывает:

1. **Тип питомца** (обязательно совпадение)
2. **Аллергии** (исключение аллергенов)
3. **Возраст** (корм и игрушки по возрасту)
4. **Вес** (размер товаров, порции корма)
5. **Порода** (специализированные товары)
6. **Кастрация/стерилизация** (специальный корм)
7. **Рейтинг и популярность** товаров

## 🚢 Развертывание в Яндекс Облаке

### Шаг 1: Подготовка образов

```bash
# Сборка образов
docker build -t petshop-backend:latest ./backend
docker build -t petshop-frontend:latest ./frontend

# Загрузка в Container Registry Яндекс Облака
docker tag petshop-backend:latest cr.yandex/<registry-id>/petshop-backend:latest
docker tag petshop-frontend:latest cr.yandex/<registry-id>/petshop-frontend:latest
docker push cr.yandex/<registry-id>/petshop-backend:latest
docker push cr.yandex/<registry-id>/petshop-frontend:latest
```

### Шаг 2: Развертывание

1. Создайте виртуальные машины в Yandex Compute Cloud
2. Установите Docker и Docker Compose
3. Настройте переменные окружения
4. Запустите с помощью `docker-compose up -d`
5. Настройте Load Balancer для распределения нагрузки

### Переменные окружения для production

**Backend (.env):**
```env
NODE_ENV=production
PORT=5000
FRONTEND_URL=https://your-domain.ru
```

**Frontend (.env.local):**
```env
NEXT_PUBLIC_API_URL=https://api.your-domain.ru/api
```

## 📝 Моковые данные

Проект включает:
- **56 товаров** из 8 категорий
- **22 породы** собак и кошек с характеристиками
- **8 категорий** товаров
- Полная информация о товарах (бренд, цена, рейтинг, аллергены, размеры)

## 🔐 Безопасность

⚠️ **Важно**: Текущая реализация использует моковую авторизацию для демонстрации UI/UX.

Для production необходимо:
- Реальная аутентификация с JWT
- База данных вместо JSON файлов
- HTTPS для всех запросов
- Валидация и санитизация данных
- Rate limiting
- CORS настройка

## 🤝 Вклад в проект

Проект создан для демонстрации современного fullstack приложения с Material Design 3.

## 📄 Лицензия

Этот проект создан в образовательных целях.

## 👨‍💻 Автор

Разработано для демонстрации навыков fullstack разработки с использованием современных технологий.

---

**Приятного использования Pet Shop! 🐾**


